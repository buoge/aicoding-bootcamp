# Instructions

需求是在Python下面创建一个Postgres的mcp：用户可以给特定自然语言描述的查询的需求，然后mcp server 根据结果来返回一个SQL或者返回这个查询的结果。
mcp的服务器在启动的时候，应该读取它都有哪些可以访问的数据库，并且缓存这些数据库的schema：了解每一个数据库下面都有哪些 table/view/types/index 等等，
然后根据这些信息以及用户的输入去调用 kimi-k2 的大模型（kimi-k2-thinking）来生成 SQL， 这是 kimi-k2 的 api 文档 https://platform.moonshot.cn/docs/overview 

mcp server需要校验这个sql只允许查询的语句，然后测试这个sql确保它能够执行并且返回有意义的结果：这里也可以把用户的输入生成的sql以及返回的结果的一部分调用 kimi-k2 来确认这样可以确保它的结果是不是有意义。

最后根据用户的输入返回相应的SQL，同时还要返回SQL查询之后的结果，根据这些需求帮我构建一个详细的需求文档，

先不要著急去做设计，等我review完这个需求文档之后呢我们再来讨论设计，文档放在 ./specs/w5/0001-pg-mcp-prd.md 文件中。

## 设计文档
根据 ./specs/w5/0001-pg-mcp-prd.md 文档，使用 FastMCP、Asyncpg、SQLGlot、Pydantic以及openai 构建 pg-mcp 的设计文档，文档放在 ./specs/w5/0002-pg-mcp-design.md 文件中。

## 实现计划
操作 1：
根据 ./specs/w5/0002-pg-mcp-design.md 文档，构建 pg-mcp 的实现计划，think ultra hard，文档放在 ./specs/w5/0004-pg-mcp-impl-plan.md 文件中。

操作 2：
 review ./specs/w5/0004-pg-mcp-impl-plan.md 文件，并构建 ./specs/w5/0005-pg-mcp-impl-plan-review.md 
 （review 最好使用 codex-cli 进行，我这里没有配置 后续计划：编码时 claudecode+kmini-k2, 审核是 codex+minimix）

## 实现 pg-mcp
根据 ./specs/w5/0004-pg-mcp-impl-plan.md 和 ./specs/w5/0002-pg-mcp-design.md 文档，使用 sub agent 完整实现 pg-mcp phase 0-4。代码放在 ./w5/pg-mcp 下。

根据 ./specs/w5/0004-pg-mcp-impl-plan.md 文档，完整实现 pg-mcp phase 5。代码放在 ./w5/pg-mcp 下。

<原有的阶段实现不了，不知道啥原因，这里跳过了一些冗余步骤，很有可能是我手动调整了一些内容导致设计文档和实现文档内容歧义，agent 不知道咋办了>

## test plan
根据 ./specs/w5/0004-pg-mcp-impl-plan.md 和 ./specs/w5/0002-pg-mcp-design.md 文档，构建 pg-mcp 的测试计划，think ultra hard，文档放在 ./specs/w5/0007-pg-mcp-test-plan.md 文件中。

## test db data
根据 @specs/w5/0001-pg-mcp-prd.md 在 ./w5/pg-mcp/fixtures 下构建三个有意义的数据库，分别有少量，中等量级，以及大量的 table/view/types/index 等schema，且有足够多的数据。生成这三个数据库的 sql 文件，并构建 Makefile 来重建这些测试数据库。

 你来建立连接和写入数据确保这几个数据库确保可用

 ## pg-mcp 运行
1. 需要提前 运行 pip install 安装 全部涉及到的库包，不然不能正常启动
2. 要通过 Claude Desktop 使用 pg-mcp：
   cd /Users/buoge/Desktop/github/aicoding-bootcamp/w5/pg-mcp && python http_server.py
```
MCP 工具已就绪:
  所有 4 个 MCP 工具都已配置并可使用：
  1. query_database - 自然语言查询
  2. execute_sql - 安全 SQL 执行
  3. list_databases - 列出数据库
  4. refresh_schema - 刷新模式
  服务器已完全准备就绪！ 🚀
```

## mcp-skill
### 配置命令行代理：
export https_proxy=http://127.0.0.1:7897;export http_proxy=http://127.0.0.1:7897;

### 安装这个 market：
 * https://github.com/anthropics/skills
 * install document-skill

### 构建 postgres 查询的 skill:
在当前项目下创建一个新的 claude skill，要求：

首先通过 psql (localhost:5432, postgres, postgres) 探索这几个数据库：simple_test、medium_test、complex_test，了解它们都有哪些 table/view/types/index 等等，每个数据库一个 md 文件，作为 skill 的 reference。
用户可以给特定自然语言描述的查询的需求，skill 根据用户输入找到相应的数据库的 reference 文件，然后根据这些信息以及用户的输入来生成正确的 SQL。SQL只允许查询语句，不能有任何的写操作，不能有任何安全漏洞比如 SQL 注入，不能有任何危险的操作比如 sleep，不能有任何的敏感信息比如 API Key 等。
使用 psql 测试这个 SQL 确保它能够执行并且返回有意义的结果。如果执行失败，则深度思考，重新生成 SQL，回到第 3 步。
把用户的输入，生成的 SQL，以及返回的结果的一部分进行分析来确认结果是不是有意义，根据分析打个分数。10分非常 confident，0分非常不 confident。如果小于 7 分，则深度思考，重新生成 SQL，回到第 3 步。
最后根据用户的输入是返回 SQL 还是返回 SQL 查询之后的结果（默认）来返回相应的内容

```
```
 



